node_modules/
dist/

# Web UI (separate package in web/)
web/node_modules/
web/dist/

# Environment — never commit real credentials
.env
credentials.env
*.credentials.env

# Generated skill output (local runs)
~/.openclaw/

# OS / tooling
.DS_Store
Thumbs.db
*.tmp
*.temp
*~
*.log
# Test coverage reports (root only — do not use bare `coverage/` or it ignores src/coverage/)
/coverage/

# Editor
.vscode/
.idea/
*.swp

# Local-only notes (roadmap, scratch) — not committed
*.local.md

# Windows: copy scripts/local-env.example.ps1 → scripts/local-env.ps1 (prepend Node to PATH)
scripts/local-env.ps1
